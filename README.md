# ChainList-Ledger

A clean-room implementation of a blockchain state engine designed entirely on top of a Doubly Linked List data structure. This framework exposes the internal link mechanics of blocks without relying on standard Python container arrays.
